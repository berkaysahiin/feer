P0 - API Ergonomics (next)
- Add `operator bool()` (or `has_value()`) for concise state checks.
- Add `value_or(default_value)` for value fallback paths.
- Add `map(fn)` to transform success values.
- Add `and_then(fn)` to chain operations returning `Result<U>`.
- Add `map_error(fn)` to transform `Err` while preserving context.

P1 - Ergonomics Tests
- Add tests for `operator bool()` and `value_or` on ok/err states.
- Add tests for `map` on value and error states (no-op on error).
- Add tests for `and_then` success chaining and early error propagation.
- Add tests for `map_error` behavior and source_location preservation.

P2 - Packaging
- Keep header include flow as default (`#include <feer/result.hpp>`).
- Add optional C++20 module interface (`modules/feer.cppm`) as nice-to-have.
- Ensure module path is additive and does not break header users.

P3 - Project Hygiene
- Add `README.md` with quickstart, local scripts, and API examples.
- Add `LICENSE`.
- Add `.gitignore` for `build/` and temporary artifacts.
